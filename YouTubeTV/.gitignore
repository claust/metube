# Credentials (real values live here — never commit)
Config/Secrets.xcconfig

# TestFlight credentials: the App Store Connect API key ids, and the .p8 itself
# if it is ever kept in-tree rather than in ~/.private_keys.
.testflight.env
private_keys/

# Generated by xcodegen — run `xcodegen generate`
YouTubeTV.xcodeproj/

# Xcode / build output
build/
build-device/
build-archive/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store

# Generated by xcodegen from project.yml (single source of truth)
Sources/Resources/Info.plist
TopShelf/Info.plist
